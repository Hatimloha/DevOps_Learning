apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "02-project.fullname" . }}-test-connection"
  labels:
    {{- include "02-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "02-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
