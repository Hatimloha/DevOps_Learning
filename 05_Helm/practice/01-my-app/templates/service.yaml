apiVersion: v1
kind: Service
metadata:
  name: {{ include "01-my-app.fullname" . }}
  labels:
    {{- include "01-my-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "01-my-app.selectorLabels" . | nindent 4 }}
