apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Release.Name }}

spec:
  selector:
    app: {{ .Values.labels.matchLabels.app }}

  ports:
    - port: {{ .Values.port}}

      targetPort: {{ .Values.port}}

  type: ClusterIP
    