apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-reader-binding
  namespace: dev

subjects:
  - kind: ServiceAccount
    name: developer # login user

roleRef:
  kind: Role
  name: pod-reader # role name

  apiGroup: rbac.authorization.k8s.io