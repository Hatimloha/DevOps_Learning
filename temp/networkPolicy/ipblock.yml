apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ipblock-networkpolicy
spec:
  podSelector:
    matchLabels:
      app: backend
  policyTypes:
    - Ingress
  ingress:
    - from:
        - ipBlock:
            cidr: 10.0.0.0/24
