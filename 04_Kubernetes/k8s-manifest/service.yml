apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx

spec:
  selector:
      app: nginx

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007

  type: LoadBalancer