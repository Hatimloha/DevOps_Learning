apiVersion: v1
kind: Service
metadata:
  name: static-website-service
  namespace: project1

spec:
  selector:
    app: static-website

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort